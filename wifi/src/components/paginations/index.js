import GsPaginationsComponent from './Paginations.vue'

const GsPaginations = {
    install: function (Vue) { 
        Vue.component('GsPaginations', GsPaginationsComponent)
     }
}

export default GsPaginations